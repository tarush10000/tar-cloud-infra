---
name: Deploy Applications

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Deploy all applications
        run: |
          cd /srv/tar-cloud-infra
          chmod +x scripts/*.sh
          ./scripts/deploy-apps.sh
